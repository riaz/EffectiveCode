#include "includes/hellofunc.h"

// note: we needed to include hellofunc twice
// once inside the definition
// once during the calling

int main() {
    printHelloMake(); // we will just call the function which has a include
    return 0; // and just return 
}